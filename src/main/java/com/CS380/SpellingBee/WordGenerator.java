package com.CS380.SpellingBee;

public class WordGenerator {

	/*
	 * This is a comment in the new Word Generator!
	 * 
	 * 
	 * After each pull, we need to right click on the project then select -> run as -> maven clean
	 * then select project clean from the menu at the top
	 * then the project should be up to date with the files in the filesystem!
	 */
}
